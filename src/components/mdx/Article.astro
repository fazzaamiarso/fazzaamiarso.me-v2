---
---

<article
  class='blog prose prose-zinc col-span-3 prose-headings:scroll-mt-20 prose-code:font-code dark:prose-invert max-lg:col-span-4 md:prose-lg'>
  <slot />
</article>

<style is:global lang='scss'>
  .blog {
    p {
      @apply dark:text-zinc-300
    }

    a {
      @apply break-all  transition-all;
    }

    a:not(.notes a) {
      @apply dark:text-primary text-primaryDarker;
      text-decoration: none;
      background-image: linear-gradient(#8761ff, #8761ff);
      background-repeat: no-repeat;
      background-position: left bottom;
      background-size: 0% 2px;
      &:hover {
        background-size: 100% 2px;
      }
    }

    img {
      @apply mx-auto rounded-md;
    }

    code {
      counter-reset: step;
      counter-increment: step 0;
      & .line::before {
        @apply text-zinc-400;
        content: counter(step);
        counter-increment: step;
        margin-right: 1.25rem;
      }
      &::after,
      &::before {
        content: "";
      }
    }

    pre {
      .blog :not(&) code {
        @apply rounded-sm bg-gray-200 px-1 py-[6px] transition-colors dark:bg-[#20272f];
      }
      code {
        font-size: 0.9rem;
      }
      &::-webkit-scrollbar {
        @apply h-1 rounded-b-md bg-gray-900;
      }
      &::-webkit-scrollbar-thumb {
        @apply my-1 rounded-full bg-zinc-400;
      }
    }
  }
</style>
